.vercel
__pycache__/
data/*.sqlite
data/*.sqlite-*
.env
.DS_Store

# Local TLS material. The CA key can mint certificates any device that
# trusts it will accept, so it never leaves this machine.
certs/
